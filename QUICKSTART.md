# Agent Workbench 5 分钟极速上手指南

本指南帮助你快速安装、配置并使用 **Agent Workbench IntelliJ 插件**。  
插件专为多仓、大模型协同研发工作流打造，助你**无需频繁切换 Git 分支、无需在多个代码仓之间跳转**，在 IDEA 中一屏洞悉需求规范、任务拆解与全仓代码变更。

---

## 步骤 1：安装插件

1. 打开 IntelliJ IDEA（或兼容的 JetBrains IDE），进入设置：
   * **macOS**：`IntelliJ IDEA` ➔ `Settings...`（快捷键 `Cmd + ,`）
   * **Windows / Linux**：`File` ➔ `Settings...`（快捷键 `Ctrl + Alt + S`）
2. 在左侧导航栏选择 **Plugins（插件）**；
3. 点击右上角的 ⚙️ 齿轮图标，选择 **Install Plugin from Disk...（从磁盘安装插件）**；
4. 选择本项目的构建包（如 `build/distributions/agent-workbench-intellij-0.2.zip`）；
5. 安装完成后根据提示重启 IDE。

---

## 步骤 2：准备工作区目录结构

为了充分发挥跨多仓库 Git 原生协同的能力，**强烈推荐使用包含工作流仓与各业务仓的“父目录”形式**打开 IDEA：

```text
my-project-workspace/              # 用 IDEA 打开此父目录
├── agent-workbench/               # 工作流 Kit 仓（包含 .workspace/ 与需求文档）
│   ├── .workspace/
│   │   └── workspace.json         # 登记已接入的业务代码仓
│   └── docs/development/features/ # 需求规范文件（requirements.md 等）
├── order-service/                 # 业务代码仓 A（后端微服务）
├── user-service/                  # 业务代码仓 B（用户服务）
└── frontend-dashboard/            # 业务代码仓 C（前端管理端）
```

> **提示**：父目录模式下，请确保 IDEA 已接入各子仓的 Git 根目录（可在 `Settings | Version Control | Directory Mappings` 中查看）。如未接入，在插件界面点击仓库时也会主动提示一键“接入并继续”。

---

## 步骤 3：打开工作台与自动识别

1. 在 IDEA 窗口左侧边栏找到并点击 **Agent Workbench** 图标打开工具窗口；
2. **自动探测机制**：
   * 插件会自动检查当前工程根目录以及子目录中是否存在 `agent-workbench/scripts/kit.py`；
   * 找到后将**自动建立绑定并加载工作区**，无需手动输入路径；
3. 如果未自动发现，只需在 Tool Window 顶部的配置区填写：
   * **Kit 根目录**：`/path/to/my-project-workspace/agent-workbench`
   * **Python 路径**：`python3`
   * 点击 **「绑定并刷新」**；
4. 顶部呈现绿色状态点 `● 运行正常` 后，点击右侧的 **「进入工作台」**（或通过快捷键 `Tools | 打开 Agent Workbench 工作台`），即可打开居中的主画布。

---

## 步骤 4：主页全景（首屏即览，无需滚动）

在左侧需求列表中点击任意 Feature（例如 `order-export-optimization` / 订单导出功能优化），中央编辑区将打开对应的需求工作台。

在 **「总览与说明」** 首页中，你可以在**同一个屏幕内完全免滚动**掌握需求的三个核心维度：

```text
┌──────────────────────────────────────┬──────────────────────────────────────┐
│  📑 需求概述 (PRD 实时渲染)           │  📋 任务拆解与执行进度                │
│                                      │  [✓] [T01] 导出接口参数设计          │
│  - 业务背景与用户价值                 │  [✓] [T02] 后端异步任务改造          │
│  - 核心接口定义与数据流               │  [ ] [T03] 前端下载进度条组件        │
│  - 验收准则与非功能要求               │                                      │
│                                      ├──────────────────────────────────────┤
│                                      │  📦 关联代码仓与变更                 │
│  (支持平滑滚动的 Markdown 高保真排版) │  - order-service [在工程定位]        │
│                                      │    develop ➔ feat/order-export       │
│                                      │  - frontend-dashboard                │
│                                      │    main ➔ feat/download-ui           │
└──────────────────────────────────────┴──────────────────────────────────────┘
```

* **左侧 58%**：完整加载当前需求的 `requirements.md`，支持原生样式渲染，长文可在内部独立平滑滚动；
* **右上 50%**：展示结构化任务拆解清单，进度比例一览无余；
* **右下 50%**：列出该需求关联的所有业务代码仓、绑定的基线分支与工作分支，以及当前 Git 现场状态。

---

## 步骤 5：跨仓比对代码（无需切换本地检出分支）

切换到 **「代码变更与比对」** 标签页：

1. **选择关联仓库**：在顶部的【关联仓库】下拉菜单中选择你想查看的微服务（例如 `order-service`）；
2. **纯净非检出比对**：
   * 即使你本地由于排查线上问题正切在 `test` 分支或 `bugfix` 分支上，也**完全不需要执行 `git checkout`**；
   * 插件会直接基于底层 Git Object 提取 **基线分支 ➔ 需求分支** 的纯净改动；
3. **查看单文件比对**：
   * 双击【变更文件】列表中的任何文件（或按回车），立即呼出 IDEA 原生双栏 Diff 窗口（左侧为基线版本，右侧为需求分支版本，新增代码绿色高亮）；
4. **全仓已提交 Diff**：
   * 点击右上角的 **`[⇥ 打开已提交 Diff]`** 按钮，直接唤起 IntelliJ 原生双分支比较器，完整查看所有提交记录与修改树。

---

## 步骤 6：基线分支保持最新（安全 Fetch）

团队协作中，同事可能不断往远程基线（如 `develop`）合并 PR。

* 当需要用最新的团队代码进行比对时，只需点击关联仓库旁的 **`[🔄 抓取远程 (Fetch)]`** 按钮；
* 插件会在后台向远程仓库执行安全抓取（更新本地的 `origin/develop` 索引）：
  * **0 影响**：不改写你的工作区代码，不触碰本地未暂存的改动；
  * **0 冲突**：纯下载远程提交对象，不执行任何 merge 操作，绝无冲突可能；
* Fetch 结束后，插件将**自动无感重新计算**需求分支相对最新远程基线的差异文件！

---

## 常见快捷操作一览

| 目标操作 | 快捷入口 |
| :--- | :--- |
| 打开工作台 | 菜单栏 `Tools` ➔ `打开 Agent Workbench 工作台`（或按双击 Shift 搜索） |
| 展开/折叠内部侧栏 | 编辑区右上角 `[折叠侧栏]` 按钮（便于笔记本小屏专注阅读） |
| 打开源 Markdown 文件 | 需求头部右上角 `查看文档 (F4)` |
| 在工程目录树中定位需求 | 需求头部右上角 `在工程中定位` |
| 重新抓取基线最新提交 | 代码变更页右上角 `[🔄 抓取远程 (Fetch)]` |
