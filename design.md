# Agent Workbench IntelliJ 插件 UI/UX 设计规范 (v2.0)

> 基于 JetBrains New UI（2024+）设计系统与最新高保真原型提炼，旨在建立严谨、清晰、工业级的插件视觉规范与交互契约。
> 本文件为当前插件所有 UI 页面重构与精细化排版的最高指导依据。

---

## 一、全局设计系统与令牌（Design Tokens）

### 1.1 配色系统（Color Palette）

| 语义层级 | Dark (Darcula / New UI) | Light (Day Theme) | 适用场景 |
| :--- | :--- | :--- | :--- |
| **Window/Editor Bg** | `#1E1F22` | `#F7F8FA` | 主编辑区全屏画布背景 |
| **ToolWindow Bg** | `#2B2D30` | `#EBECF0` | 左侧工具导航窗口背景 |
| **Surface Card** | `#26282B` (微浮层) | `#FFFFFF` | 卡片容器、浮层背景 |
| **Border / Divider** | `#393B40` | `#D1D4DC` | 1px 容器边框、列表分割线 |
| **Accent Primary** | `#3574F0` (JetBrains Blue) | `#3574F0` | 核心选中高亮、主按钮、激活 Tab |
| **Selection Bg** | `#273850` (深蓝通透度) | `#E5EEFC` | 列表项选中底色 |
| **Success Green** | `#59A869` | `#36874E` | 已完成任务、健康状态正常、通过项 |
| **Warning Amber** | `#E5A158` | `#B8771B` | 警告、进行中/测试中、需要关注 |
| **Danger Red** | `#DB5C5C` | `#C7384A` | 错误、失败、代码删除统计行 |
| **AI Purple Accent** | `#9D7CD8` (或 `#B99BF8`) | `#7D57C1` | AI 复制提示词徽标、智能体扩展操作 |
| **Text Primary** | `#DFE1E5` | `#1E2024` | 标题、激活文本、关键数值 |
| **Text Secondary** | `#9DA0A8` | `#6C707E` | 次级描述、辅助说明、标签 |
| **Text Muted/Code** | `#6F737A` | `#9094A0` | 路径、面包屑、代码增减统计 |

### 1.2 字体排版（Typography）

- **UI 正文与标签**：`Inter` / `JetBrains Sans` / 系统原生 UI 字体。
  - 大标题（Header Title）：`18pt` Bold
  - 次级小节标题（Section Header）：`14pt` SemiBold
  - 正文与表单（Body & Form）：`12pt` Regular
  - 辅助说明与徽章（Caption & Badge）：`10.5pt` Medium
- **代码与标识符**：`JetBrains Mono` / 等宽字体。
  - 适用：Git 分支名、Feature Slug、任务 ID（`[T01]`）、增减行数统计（`+35 -4`）、代码类名引用。

### 1.3 容器与几何规范（Geometry & Radius）

- **边框与分割**：一律使用 `1px` 纯色描边（不加模糊阴影）。
- **圆角（Corner Radius）**：
  - 按钮与输入框：`4px`
  - 状态胶囊/Badge：`4px`
  - 卡片与横幅 Banner：`6px`
  - 进度条轨道：`3px`

---

## 二、左侧工具窗口规范（ToolWindow Navigation）

### 2.1 顶部工作区卡片（Workspace Card）
- **布局**：顶部 2 行紧凑结构。
  - 第 1 行：工作区名称（`13pt` Bold）+ 右侧 `● 运行正常` 绿色状态圆点。
  - 第 2 行：4 宫格水平导航按钮组（`总览`、`记录`、`体检`、`配置`），带图标与文字，等分平铺排列，悬浮（Hover）时呈现 `#34373E` 微底色。

### 2.2 分支感知横幅（Branch Context Banner）
- **触发条件**：当前 Git 检出分支与某个 Feature 绑定的分支匹配时展示。
- **视觉**：背景 `#1E2B38`，细边框 `#2A3E52`，内边距 `8px 12px`，圆角 `6px`。
- **内容**：`💡 当前检出分支属于需求: <slug>` + 右侧操作按钮 `[切换查看]`。

### 2.3 需求列表（Feature List）
- **搜索栏**：全宽 `SearchTextField`，右侧带清除图标，高度 `30px`。
- **列表项（Card Item）**：
  - 默认态：透明背景，下衬 `1px` 分割线。
  - 选中态：底色 `#273850`，左侧带有蓝色圆角高亮。
  - 结构：
    - 左侧：需求图标 + 需求 Slug（Bold） + 下方浅灰需求中文描述。
    - 右侧：双胶囊徽章——`[📍 当前检出]`（蓝色胶囊） + `[进行中 / 已完成 / 测试中]`（语义色胶囊）。

### 2.4 底部业务仓库折叠区（Repositories Section）
- **标题**：`已登记代码仓库 (N)` + 折叠/展开箭头。
- **条目**：代码仓文件夹图标 + 仓库名 + 分支小图标及检出分支名称。
- **底栏状态**：`● 工作区已连接` + 右下角设置齿轮按钮。

---

## 三、中央全屏编辑工作台规范（Editor Tab Canvas）

### 3.1 头部（Feature Header）
- **面包屑导航**：`Feature > <slug>`（`11pt` 浅灰，可点击回退）。
- **主标题**：需求完整中文标题（`18pt` 粗体）。
- **元数据胶囊组**：
  - 状态胶囊（如 `development` 蓝灰底色）；
  - 流程阶段（如 `feature.implement`）；
  - 任务进度胶囊（如 `4/7 已完成 (57%)`）。
- **右上角操作组**：
  - ✨ **复制 AI 提示词 (Copy Prompt)**：带剪贴板图标的标准次级按钮，支持一键复制并弹出通知。
  - **查看文档 (F4)**：快捷打开源 Markdown。
  - **在工程中定位**：快速在 Project View 中定位对应 specs 目录。
  - **刷新**：同步最新磁盘数据。

### 3.2 工作区健康体检横条（Doctor Status Strip）
- **位置**：常驻挂载于 Header 与 Tab 切换栏之间。
- **视觉**：
  - 正常：背景 `#1A2A22`，边框 `#274A36`，前景色 `#59A869`，“✅ 工作区体检正常：所有 N 个代码仓配置完备 · 0 项错误 [查看体检详情]”。
  - 警告/异常：背景 `#2E2318`，边框 `#594126`，前景色 `#E5A158`。

### 3.3 下划线风格子标签页（Sub-Tabs）
- **标签项**（与实现一致）：`概览` | `文档` | `计划` | `变更` | `验证` | `流程`。
- **交互态**：无卡片外框，激活项下方绘制 `2px` JetBrains Blue（`#3574F0`）指示横线，非激活项文字浅灰，悬浮时文字变白。

---

## 四、主工作区双栏画布规范（Dashboard Two-Column Canvas）

### 4.1 左栏：需求概述与 PRD 文档预览
- **容器**：深色内嵌卡片，四周 `1px` 边框，内边距 `16px 20px`。
- **结构化排版**：
  - `需求概述`（标题右侧标注 `PRD · v1.3` 等版本元数据）；
  - `需求目标`：无序列表与项目符号；
  - `降级策略 / 核心规则`：有序步骤编号；
  - `相关代码`：高亮可交互类名胶囊（如 `PaymentClient.kt`，带源码跳转图标）；
  - `架构示意`：轻量流程/时序示意图容器（支持文本渲染或原生组件绘制）。

### 4.2 右栏上：任务拆解看板（Task Checklist）
- **标题行**：`任务拆解` + `4 / 7` 计数 + **进度条（Progress Bar）** + `57%` 进度百分比。
  - 进度条：高度 `4px`，背景深灰，已完成部分高亮翠绿色（`#59A869`）。
- **任务条目（Task Row）**：
  - 已完成任务：绿色带勾复选框、文字带删除线、任务 ID 灰色。
  - 当前聚焦任务：外包淡蓝色 `1px` 描边卡片（`#273850` 底色），任务 ID `[T05]` 等宽字体高亮，显示详细实现要求。
  - 未开始任务：常规灰色复选框与文字。

### 4.3 右栏下：跨代码仓变更面板（Code Changes & Diff）
- **标题行**：`代码变更` + 统计摘要（如 `3 个仓库 · 6 个文件`）。
- **文件列表**：当前实现为平铺文件名列表（`FeatureChangesPanel`），双击打开非检出 Diff。
  - 规划中（尚未实现）：按仓库折叠分组、原生文件图标、右对齐增减行数徽章（`+35` / `-4`）。
- **底部快捷操作条（Action Footer）**：
  - 左右平铺按钮：`🔍 查看 Diff 比对 (Enter)` 与 `📁 在 Project 视图打开`。
